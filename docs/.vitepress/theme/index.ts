import DefaultTheme from 'vitepress/theme';
import './custom.css';

// Extend the default VitePress theme with our own CSS tweaks
// (wider content for the scenario tables, nicer table styling).
export default DefaultTheme;
